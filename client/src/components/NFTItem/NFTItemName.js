/** @jsxImportSource @emotion/react */

const NFTItemName = (props) => {
  const { children } = props;

  return <p css={{ fontSize: '12px', fontWeight: 600, marginTop: '8px' }}>{children}</p>;
};

export default NFTItemName;
