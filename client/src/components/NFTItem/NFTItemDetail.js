/** @jsxImportSource @emotion/react */

const NFTItemDetail = (props) => {
  const { children } = props;

  return <p css={{ fontSize: '12px', fontWeight: 600 }}>{children}</p>;
};

export default NFTItemDetail;
